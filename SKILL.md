---
name: mas4paper
description: |
  多智能体协作学术论文生成系统（MAS4Paper）。用于从选题到排版的学术论文全流程自动化生成。
  
  触发场景：
  - 用户要求生成学术论文、研究论文、学术文章
  - 用户提到"写论文"、"生成论文"、"学术论文自动生成"
  - 用户要求进行学术研究并输出论文成果
  - 用户提到"多智能体协作"、"MAS4Paper"、"智能科研范式"
  
  系统支持：经济学与交叉学科研究，全流程自主完成论文生成。
---

# MAS4Paper - 多智能体协作学术论文生成系统

## 概述

本系统是一套面向经济学与交叉学科研究的多智能体协作系统，能够从选题、数据采集、处理、理论建模、实证分析、图表绘制、论文写作、独立审稿、修改迭代到最终排版，**全流程自主完成一篇学术论文的生成**。

## 核心特性

- **全流程自动化**：从选题到排版，无需人工干预
- **11个专业智能体**：分工明确，协同工作
- **可复现性保障**：所有中间数据、代码、过程完整记录
- **标准化接口**：智能体间通过共享知识库协作

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    编排层 (Orchestrator)                      │
│                  任务调度 + 流程控制 + 冲突解决                   │
└─────────────────────────────────────────────────────────────┘
                              │
       ┌──────────────────────┼──────────────────────┐
       ▼                      ▼                      ▼
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   研究层    │      │   写作层    │      │   质控层    │
│  Agent 1-5  │      │  Agent 6-8  │      │  Agent 9-11 │
└─────────────┘      └─────────────┘      └─────────────┘
       │                      │                      │
       └──────────────────────┼──────────────────────┘
                              ▼
                    ┌─────────────────┐
                    │   共享知识库     │
                    │  数据 + 代码    │
                    │  中间产物 + 日志 │
                    └─────────────────┘
```

## 11个专业智能体

### 研究层（Agent 1-5）

| Agent | 名称 | 职责 | 输入 | 输出 |
|-------|------|------|------|------|
| Agent 1 | **选题策划Agent** | 分析研究前沿，确定选题方向 | 研究领域 + 关键词 | 选题报告 + 文献综述方向 |
| Agent 2 | **数据采集Agent** | 数据收集、清洗、标注 | 数据源清单 | 清洗后数据集 + 数据字典 |
| Agent 3 | **理论建模Agent** | 理论框架构建、假设推导 | 选题报告 + 文献 | 理论模型 + 假设清单 |
| Agent 4 | **实证分析Agent** | 统计分析、回归、因果推断 | 数据 + 模型 | 实证结果 + 稳健性检验 |
| Agent 5 | **图表绘制Agent** | 可视化图表生成 | 分析结果 | 出版级图表 + 图注 |

### 写作层（Agent 6-8）

| Agent | 名称 | 职责 | 输入 | 输出 |
|-------|------|------|------|------|
| Agent 6 | **结构规划Agent** | 论文结构设计、章节安排 | 研究成果 | 论文大纲 + 写作指南 |
| Agent 7 | **内容撰写Agent** | 各章节内容撰写 | 大纲 + 材料 | 论文初稿 |
| Agent 8 | **排版格式Agent** | 格式调整、参考文献、排版 | 论文初稿 | 排版后论文 |

### 质控层（Agent 9-11）

| Agent | 名称 | 职责 | 输入 | 输出 |
|-------|------|------|------|------|
| Agent 9 | **独立审稿Agent** | 同行评审模拟、问题发现 | 论文初稿 | 审稿意见 + 修改建议 |
| Agent 10 | **修改迭代Agent** | 根据审稿意见修改 | 审稿意见 | 修改后论文 |
| Agent 11 | **质量检验Agent** | 最终质量检查、复现性验证 | 完整论文 | 质量报告 + 发布版本 |

## 工作流程

### 阶段一：研究准备（Day 1-3）

```
1. Agent 1 接收研究领域 → 生成选题报告
2. Agent 2 根据选题 → 启动数据采集
3. Agent 3 根据选题 + 文献 → 构建理论模型
4. 编排器协调进度，解决冲突
```

### 阶段二：实证分析（Day 4-6）

```
1. Agent 4 获取数据 + 模型 → 执行实证分析
2. Agent 5 根据分析结果 → 生成图表
3. 所有中间产物存入共享知识库
4. 编排器监控质量，必要时回退
```

### 阶段三：论文撰写（Day 7-10）

```
1. Agent 6 根据研究成果 → 设计论文结构
2. Agent 7 按章节撰写 → 生成初稿
3. Agent 8 格式调整 → 完成排版
```

### 阶段四：质量迭代（Day 11-14）

```
1. Agent 9 独立审稿 → 输出审稿意见
2. Agent 10 根据意见修改 → 迭代论文
3. Agent 11 最终检验 → 输出发布版本
4. 所有过程文件打包 → 可复现包
```

## 执行指令

当用户触发本技能时，按以下步骤执行：

### Step 1: 确认研究需求

询问用户：
- **研究领域**：经济学哪个分支？或交叉学科方向？
- **选题偏好**：有特定选题方向吗？还是系统自动推荐？
- **数据要求**：有指定数据源吗？需要采集哪些数据？
- **目标期刊**：计划投稿哪个期刊？（影响格式要求）
- **时间要求**：预计完成时间？

### Step 2: 启动智能体协作

根据用户需求，按流程启动智能体：

```
# 示例：启动选题策划Agent
你现在是【选题策划Agent】，请根据以下信息生成选题报告：

研究领域：[用户指定领域]
关键词：[用户指定或自动提取]
研究前沿：[搜索最新文献]

请输出：
1. 选题报告（研究问题、创新点、预期贡献）
2. 文献综述方向（核心文献清单）
3. 数据需求清单
4. 预期时间线
```

### Step 3: 协调智能体工作

每个Agent完成任务后：
1. 将输出存入共享知识库（文件形式）
2. 编排器检查输出质量
3. 触发下游Agent
4. 记录所有中间产物

### Step 4: 迭代优化

- 审稿Agent发现问题 → 触发修改Agent
- 质量检验不通过 → 回退到对应Agent重做
- 用户中途干预 → 暂停并响应用户需求

### Step 5: 输出交付

最终交付物：
```
/output/
├── paper_final.docx        # 最终论文
├── paper_final.pdf         # PDF版本
├── figures/                # 所有图表
├── data/                   # 数据集 + 数据字典
├── code/                   # 完整代码
│   ├── analysis.py         # 实证分析代码
│   ├── figures.py          # 图表生成代码
│   └── robustness.py       # 稳健性检验代码
├── logs/                   # 执行日志
│   ├── agent1_selection.log
│   ├── agent2_data.log
│   └── ...
└── reproducibility/        # 可复现包
    ├── README.md           # 运行说明
    ├── environment.yml     # 环境配置
    └── requirements.txt    # 依赖包
```

## 可复现性保障机制

### 1. 全过程记录

每个Agent执行过程均记录：
- 输入参数
- 执行时间
- 中间产物
- 输出结果
- 错误日志

### 2. 数据清洗规范

```python
# 数据清洗标准流程
def clean_data(raw_data):
    """
    数据清洗标准流程
    1. 缺失值处理
    2. 异常值检测
    3. 变量标准化
    4. 数据验证
    """
    # 记录每个步骤
    log_step("缺失值处理", params)
    log_step("异常值检测", params)
    # ...
```

### 3. 代码版本控制

- 所有代码存入 `code/` 目录
- 每次修改记录版本号
- 提供完整运行环境配置

### 4. 随机种子固定

```python
import numpy as np
import random

SEED = 42
np.random.seed(SEED)
random.seed(SEED)
```

## 文件结构

工作目录结构：

```
mas4paper_workspace/
├── knowledge_base/         # 共享知识库
│   ├── selection/          # 选题相关
│   ├── data/               # 数据相关
│   ├── model/              # 模型相关
│   ├── results/            # 结果相关
│   └── drafts/             # 草稿相关
├── agents/                 # Agent配置
│   ├── agent1_selection.yaml
│   ├── agent2_data.yaml
│   └── ...
├── logs/                   # 执行日志
├── output/                 # 最终输出
└── config.yaml             # 全局配置
```

## 注意事项

1. **伦理合规**：数据采集需遵守法律法规和学术伦理
2. **原创性**：系统辅助研究，核心创新来自人类研究者
3. **验证必要性**：关键结果需人工验证
4. **期刊要求**：不同期刊有不同格式要求，注意调整

## 参考资源

详细参考文档：
- [智能体配置参考](references/agent_configs.md)
- [数据源清单](references/data_sources.md)
- [期刊格式模板](references/journal_templates.md)
- [代码规范](references/code_standards.md)
